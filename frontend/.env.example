# Variables d'environnement du frontend (Vite)
# Copier en .env.local pour le dev, ou définir dans le dashboard Vercel pour la prod.

# URL de base de l'API backend (Django sur Hostinger), SANS slash final.
# Exemple : https://api.mondomaine.com/api
# Laisser vide en dev local : les appels passent par le proxy Vite (/api).
VITE_API_URL=
